import { MainContent } from '@/shared/components/MainContent';

const Dashboard = () => {
  return (
    <MainContent>
      <div>Dashboard</div>
    </MainContent>
  );
};

export default Dashboard;
